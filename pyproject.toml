[project]
name = "qq-task-notifier"
version = "0.1.0"
description = "QQ Bot WebSocket connector and task-completion notifier"
requires-python = ">=3.11"
dependencies = [
    "aiohttp>=3.14.3",
    "qq-botpy>=1.2.1",
]

[tool.uv]
package = false
